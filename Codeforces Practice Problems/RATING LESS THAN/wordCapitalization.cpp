#include <bits/stdc++.h>
using namespace std;
#define rep(i, a, n) for (int i = a; i < n; i++)
#define per(i, a, n) for (int i = n - 1; i >= 0; i--)
#define ll long long int

int main()
{
    string str;
    cin >> str;

    str[0] = toupper(str[0]);
    cout << str << endl;

    return 0;
}
